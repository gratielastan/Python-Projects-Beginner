amount = 100
tax = 0.06
total = amount +  amount * tax
print(total) #call the print function to output total

amount = int(10.6) #convert a float to an int - chopping off the decimal part
print(amount)

amount = float(10) # convert an int to a float
print(amount)